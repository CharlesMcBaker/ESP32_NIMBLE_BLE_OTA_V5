#include "utill.h"

//#define LOG_UTILL "UTILL"
#define LOG_OTAK "OTAK"

void print_bytes(const uint8_t *bytes, int len)
{
    // int i;
    // for (i = 0; i < len; i++) 
    // {
    //     ESP_LOGI(LOG_OTAK, "%s0x%02x", i != 0 ? ":" : "", bytes[i]));
    // }
}

void print_addr(const void *addr)
{
    // const uint8_t *u8p;

    // u8p = addr;
    // MODLOG_DFLT(DEBUG, "%02x:%02x:%02x:%02x:%02x:%02x",u8p[5], u8p[4], u8p[3], u8p[2], u8p[1], u8p[0]);
}
